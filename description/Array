-------------------------------------------------------------------------------------------------
# Array.of()
    어떤 걸 array로 만들고 싶을 때 사용한다
    const friends = Array.of("jungwoo", "donggue", "mom", "father");
    console출력결과 ["jungwoo", "donggue", "mom", "father"]
-------------------------------------------------------------------------------------------------
# Array.from(array-like object)
    array-like object를 array형식으로 바꿔줌
    ex) HTMLCollection(10) 등
-------------------------------------------------------------------------------------------------
# Array.find(함수)
    true이면 찾은 것 중 첫 번째 element를 반환한다
-------------------------------------------------------------------------------------------------
# Array.findIndex(함수)
    true이면 찾은 것 중 첫 번째 element의 index를 반환한다
    false이면 -1을 반환한다
-------------------------------------------------------------------------------------------------
# Array.fill(채울 것, 시작index, 마지막index)
    시작 index부터 마지막 index까지 array를 채우는 것
-------------------------------------------------------------------------------------------------
# Array.includes()
    true, false를 반환